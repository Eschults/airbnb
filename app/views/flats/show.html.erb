<div class="col-xs-12 white-bg bord-rad-2 padding-btm-15">
  <h1><%= @flat.title %></h1>
  <% @flat.photos.each do |photo| %>
    <div class="col-xs-4 flat-picture">
      <%= image_tag photo.picture.url(:medium) %>
    </div>
  <% end %>
  <br>
  <br>
  <p><%= @flat.description %></p>
  <p><%= @flat.price %><%= @flat.currency %> la nuitée pour 2 personnes.</p>
  <p><%= link_to "Edit flat", edit_flat_path(@flat) %></p>
</div>